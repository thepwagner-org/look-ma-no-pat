name: Add issue to project

on: pull_request

permissions:
  id-token: write

jobs:
  add-to-project:
    name: Add issue to project
    runs-on: ubuntu-latest
    steps:
      - uses: thepwagner/token-action@e08090cbf8f2f15b8dc2629adb306099d398f381 # tag=v0.0.1
        id: token
        with:
          server: "https://gtfo-yuiafdvuwa-uc.a.run.app"
          repositories: |
            - thepwagner-org/look-ma-no-pat
          permissions: |
            content: write
      - run: |
          echo "${{steps.token.outputs.token}}"
